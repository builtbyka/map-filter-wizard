/* eslint-disable import/prefer-default-export */
// import { createSelector } from 'reselect';

export const postcodes = ['generation', 'demand', 'both'];
export const locationActive = (state) => state.flow.location.active;
