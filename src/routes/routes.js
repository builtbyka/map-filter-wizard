/* eslint-disable import/prefer-default-export */

const routes = {
  location: {
    next: 'features',
    prev: null,
    skip: null,
  },
  features: {
    next: null,
    prev: null,
  },
};

export default routes;
